<% if logged_in? %>
  <div class="center">
    <h1>VAE Model</h1>
    <%= link_to image_tag("numakuro.png", alt: "Numa", width: "600px"),
                        "#" %>
  </div>
  <div class="row">
    <div class="col-md-4 center">
      <%= render 'layouts/form' %>
    </div>
  </div>
<% else %>
  <div class="center jumbotron">
    <h1>VAE Sample Images Home</h1>
    <p></p>

    <%= link_to "Sign up now!", signup_path, class: "btn btn-lg btn-primary" %>
  </div>

<% end %>